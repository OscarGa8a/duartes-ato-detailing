---
import { CldImage } from 'astro-cloudinary';
import Button from './Button.astro';

interface Props {
  image: string;
  title: string;
  description: string;
  href?: string;
}

const { image, title, description, href } = Astro.props;
---

<article class="bg-white rounded-lg overflow-hidden shadow-sm flex flex-col h-full">
  <div class="aspect-video overflow-hidden min-h-70">
    <a href={href} class="block w-full h-full">
      <CldImage
        src={image}
        alt={title}
        width={600}
        height={300}
        sizes="(max-width: 640px) 100vw, 33vw"
        class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
      />
    </a>
  </div>
  <div class="p-5 flex flex-col gap-3 flex-1">
    <h3 class="text-text-dark font-bold text-lg">{title}</h3>
    <p class="text-text-muted text-sm leading-relaxed flex-1 line-clamp-4">{description}</p>
    {href && (
      <Button href={href} variant="primary" class="w-full text-center">
        See more details
      </Button>
    )}
  </div>
</article>
